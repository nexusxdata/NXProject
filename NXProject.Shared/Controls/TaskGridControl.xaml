<UserControl x:Class="NXProject.Controls.TaskGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <DataGrid x:Name="TaskGrid"
              ItemsSource="{Binding Tasks, RelativeSource={RelativeSource AncestorType=UserControl}}"
              SelectedItem="{Binding SelectedTask, Mode=TwoWay}"
              AutoGenerateColumns="False"
              AllowDrop="True"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              ScrollViewer.CanContentScroll="False"
              SelectionUnit="FullRow"
              Background="White"
              BorderThickness="0"
              RowHeight="22"
              ColumnHeaderHeight="40"
              PreviewMouseLeftButtonDown="OnTaskGridPreviewMouseLeftButtonDown"
              PreviewMouseMove="OnTaskGridPreviewMouseMove"
              DragOver="OnTaskGridDragOver"
              Drop="OnTaskGridDrop"
              BeginningEdit="OnTaskGridBeginningEdit"
              SelectionChanged="OnTaskGridSelectionChanged">

        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsHighlightedPredecessor}" Value="True">
                        <Setter Property="Background" Value="#FFEEDD"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding HasSyncConflict}" Value="True">
                        <Setter Property="Background" Value="#FFDDDD"/>
                        <Setter Property="Foreground" Value="#A4262C"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DataGrid.RowStyle>

        <DataGrid.Columns>

            <!-- ID (clicável: abre edição do vínculo DevOps) -->
            <DataGridTemplateColumn x:Name="IdColumn" Header="ID" Width="35" IsReadOnly="True">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Content="{Binding DisplayId}"
                                Click="OnIdCellClick"
                                ToolTip="Clique para editar o vínculo DevOps (ID, tipo, estado)"
                                Cursor="Hand"
                                BorderThickness="0"
                                Background="Transparent"
                                Foreground="#2B579A"
                                FontSize="11"
                                Padding="0"
                                HorizontalContentAlignment="Left"
                                VerticalContentAlignment="Center">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <TextBlock Text="{TemplateBinding Content}"
                                               Foreground="{TemplateBinding Foreground}"
                                               Margin="3,0"
                                               TextAlignment="Left"
                                               VerticalAlignment="Center"
                                               TextDecorations="Underline"/>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <!-- Tipo + Estado DevOps (compacto: ex. "FEA·A") -->
            <DataGridTemplateColumn x:Name="DevOpsColumn" Header="T·E" Width="50" IsReadOnly="True">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DevOpsTag}"
                                   Foreground="{Binding DevOpsBrush}"
                                   ToolTip="{Binding DevOpsTooltip}"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   TextAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <!-- Nome (com indentação) -->
            <DataGridTemplateColumn x:Name="NameColumn" Header="Nome da Tarefa" Width="*" MinWidth="120">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{Binding Indent}"/>
                                <ColumnDefinition Width="14"/><!-- botão expand -->
                                <ColumnDefinition Width="Auto"/><!-- ícone tipo -->
                                <ColumnDefinition Width="*"/><!-- nome -->
                            </Grid.ColumnDefinitions>

                            <!-- Botão expand/collapse — só visível em tarefas-sumário -->
                            <ToggleButton Grid.Column="1"
                                          IsChecked="{Binding IsExpanded, Mode=TwoWay}"
                                          PreviewMouseLeftButtonDown="OnHierarchyTogglePreviewMouseLeftButtonDown"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          Width="14" Height="14"
                                          Padding="0" BorderThickness="0"
                                          Background="Transparent"
                                          Cursor="Hand"
                                          Focusable="False">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <TextBlock x:Name="Arrow"
                                                               Text="▶"
                                                               FontSize="9"
                                                               Foreground="#2B579A"
                                                               VerticalAlignment="Center"
                                                               HorizontalAlignment="Center"/>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="Arrow" Property="Text" Value="▼"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSummary}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>

                            <!-- Ícone: milestone ou summary -->
                            <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="0,0,3,0"
                                       FontSize="10">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value=""/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DisplayAsMilestone}" Value="True">
                                                <Setter Property="Text" Value="♦"/>
                                                <Setter Property="Foreground" Value="Goldenrod"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- Nome (+ selo BLOCK quando bloqueado) -->
                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Margin="0,0,5,0" Padding="4,0" CornerRadius="3"
                                        Background="#FDE7E9" BorderBrush="#D13438" BorderThickness="1"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding IsBlocked, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="⛔ BLOCK" FontSize="9" FontWeight="SemiBold"
                                               Foreground="#A4262C" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSummary}" Value="True">
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{Binding Indent}"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                     BorderThickness="0" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>

            <!-- Duração -->
            <DataGridTemplateColumn x:Name="DurationColumn" Header="Dur.(h)" Width="55">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Border Padding="2,0"
                                CornerRadius="2"
                                Background="Transparent">
                            <TextBlock Text="{Binding DurationHours, StringFormat={}{0:0}}"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding UsesSfpEstimate}" Value="True">
                                                <Setter Property="Foreground" Value="#6E6E6E"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UsesSfpEstimate}" Value="True">
                                            <Setter Property="Background" Value="#F1F1F1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding DurationHours, StringFormat={}{0:0}, Mode=OneTime}"
                                 BorderThickness="0"
                                 TextAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 IsReadOnly="{Binding UsesSfpEstimate}"
                                 LostFocus="OnDurationEditLostFocus"
                                 KeyDown="OnDurationEditKeyDown">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="IsHitTestVisible" Value="True"/>
                                    <Setter Property="IsTabStop" Value="True"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UsesSfpEstimate}" Value="True">
                                            <Setter Property="Background" Value="#F1F1F1"/>
                                            <Setter Property="Foreground" Value="#6E6E6E"/>
                                            <Setter Property="IsHitTestVisible" Value="False"/>
                                            <Setter Property="IsTabStop" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>

            <!-- SFP -->
            <DataGridTextColumn x:Name="SfpColumn" Header="SFP" Binding="{Binding SfpPoints}" Width="55">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="TextAlignment" Value="Center"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <!-- Início -->
            <DataGridTemplateColumn x:Name="StartColumn" Header="Início" Width="110" SortMemberPath="Start">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StartDisplay}" VerticalAlignment="Center" Margin="3,0">
                            <TextBlock.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Retirar data fixa"
                                              Command="{Binding ClearStartFixedCommand}"
                                              IsEnabled="{Binding StartFixed}"/>
                                </ContextMenu>
                            </TextBlock.ContextMenu>
                        </TextBlock>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <TextBox x:Name="StartEditBox"
                                 Text="{Binding Start, StringFormat=dd/MM/yyyy, UpdateSourceTrigger=Explicit}"
                                 VerticalAlignment="Center"
                                 KeyDown="OnStartEditKeyDown"
                                 LostFocus="OnStartEditLostFocus"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>

            <!-- Fim -->
            <DataGridTemplateColumn x:Name="FinishColumn" Header="Fim" Width="110" SortMemberPath="Finish">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding FinishDisplay}" VerticalAlignment="Center" Margin="3,0">
                            <TextBlock.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Fixar data fim"
                                              Command="{Binding FixFinishCommand}"/>
                                    <MenuItem Header="Retirar data fixa"
                                              Command="{Binding ClearFinishFixedCommand}"
                                              IsEnabled="{Binding FinishFixed}"/>
                                </ContextMenu>
                            </TextBlock.ContextMenu>
                        </TextBlock>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <TextBox x:Name="FinishEditBox"
                                 Text="{Binding Finish, StringFormat=dd/MM/yyyy, UpdateSourceTrigger=Explicit}"
                                 VerticalAlignment="Center"
                                 KeyDown="OnFinishEditKeyDown"
                                 LostFocus="OnFinishEditLostFocus"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>

            <!-- % Completo -->
            <DataGridTemplateColumn x:Name="PercentColumn" Header="% Compl." Width="70">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Grid Margin="2,1">
                            <ProgressBar Value="{Binding PercentComplete}" Maximum="100" Height="12"
                                         Background="#E0E0E0" Foreground="#217346"/>
                            <TextBlock Text="{Binding PercentComplete, StringFormat={}{0:0}%}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       FontSize="9" Foreground="{Binding PercentCompleteTextBrush}"/>
                        </Grid>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding PercentComplete, Mode=TwoWay, UpdateSourceTrigger=LostFocus, StringFormat={}{0:0}}"
                                 BorderThickness="0"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="IsReadOnly" Value="True"/>
                                    <Setter Property="Background" Value="#F1F1F1"/>
                                    <Setter Property="Foreground" Value="#6E6E6E"/>
                                    <Setter Property="IsHitTestVisible" Value="False"/>
                                    <Setter Property="IsTabStop" Value="False"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CanEditPercentComplete}" Value="True">
                                            <Setter Property="IsReadOnly" Value="False"/>
                                            <Setter Property="Background" Value="White"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                            <Setter Property="IsHitTestVisible" Value="True"/>
                                            <Setter Property="IsTabStop" Value="True"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>

            <!-- Predecessoras -->
            <DataGridTemplateColumn x:Name="PredecessorColumn" Header="Pred." Width="55" SortMemberPath="PredecessorsText">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Content="{Binding PredecessorsText}"
                                Click="OnPredecessorCellClick"
                                ToolTip="Clique para escolher predecessoras&#x0a;Botão direito para destacar no Gantt"
                                BorderThickness="0"
                                Background="Transparent"
                                Padding="3,0"
                                HorizontalContentAlignment="Left"
                                VerticalContentAlignment="Center">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Destacar predecessoras" Click="OnHighlightPredecessorsClick"/>
                                    <MenuItem Header="Desmarcar predecessoras" Click="OnClearPredecessorHighlightClick"
                                              Visibility="{Binding IsHighlightSource, Converter={StaticResource BoolToVis}}"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <Button Content="{Binding PredecessorsText}"
                                Click="OnPredecessorCellClick"
                                ToolTip="Clique para escolher predecessoras&#x0a;Botão direito para destacar no Gantt"
                                BorderThickness="0"
                                Background="Transparent"
                                Padding="3,0"
                                HorizontalContentAlignment="Left"
                                VerticalContentAlignment="Center">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Destacar predecessoras" Click="OnHighlightPredecessorsClick"/>
                                    <MenuItem Header="Desmarcar predecessoras" Click="OnClearPredecessorHighlightClick"
                                              Visibility="{Binding IsHighlightSource, Converter={StaticResource BoolToVis}}"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>

            <!-- Recursos (editável: ComboBox com criação rápida) -->
            <DataGridTemplateColumn x:Name="ResourcesColumn" Header="Recursos" Width="100">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ResourcesText, Mode=OneWay}"
                                   VerticalAlignment="Center"
                                   Margin="3,0">
                            <TextBlock.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Editar % de alocação..."
                                              Click="OnEditPercAlocClick"/>
                                </ContextMenu>
                            </TextBlock.ContextMenu>
                        </TextBlock>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <ComboBox ItemsSource="{Binding AvailableResources, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  SelectedItem="{Binding PrimaryResource, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  DisplayMemberPath="DisplayName"
                                  IsEditable="True"
                                  StaysOpenOnEdit="False"
                                  IsTextSearchEnabled="False"
                                  DropDownClosed="OnResourceComboDropDownClosed"
                                  KeyDown="OnResourceComboKeyDown"
                                  BorderThickness="0"
                                  Padding="4,2"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>

            <!-- Sprint: mostra o NOME da sprint do DevOps; editável via dropdown -->
            <DataGridTemplateColumn x:Name="SprintColumn" Header="Sprint" Width="90">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding SprintDisplay}"
                                   TextAlignment="Left"
                                   Margin="3,0"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <ComboBox ItemsSource="{Binding AvailableSprintsForTask}"
                                  SelectedValuePath="Path"
                                  SelectedValue="{Binding SprintPath, Mode=OneWay}"
                                  IsEditable="False"
                                  DropDownOpened="OnSprintComboDropDownOpened"
                                  DropDownClosed="OnSprintComboDropDownClosed">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="SprintDates" Margin="6,0,0,0" Foreground="Gray">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="({0:dd/MM/yy} – {1:dd/MM/yy})">
                                                    <Binding Path="Start"/>
                                                    <Binding Path="End"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>
                                    <DataTemplate.Triggers>
                                        <!-- Opção "(sem sprint)": Path nulo, sem datas -->
                                        <DataTrigger Binding="{Binding Path}" Value="{x:Null}">
                                            <Setter TargetName="SprintDates" Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>

        </DataGrid.Columns>

    </DataGrid>
</UserControl>
