<Window x:Class="NXProject.Views.TfsOnlineChildTasksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Online_Title}"
        Width="1020" Height="600"
        MinWidth="800" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        Background="White">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="TitleText"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="#2B579A"/>
            <TextBlock x:Name="StatusText"
                       FontSize="11"
                       Foreground="#666"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Grid de atividades -->
        <DataGrid Grid.Row="1"
                  x:Name="ItemsGrid"
                  SelectionChanged="OnGridSelectionChanged"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0"
                  SelectionMode="Single"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  TextElement.FontSize="12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource Online_ColId}" Width="72" Binding="{Binding IdDisplay}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColType}" Width="52" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColName}" Width="230" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColStatus}" Width="80" Binding="{Binding State}"/>
                <!-- Desde quando esta no estado (StateChangeDate): o hint traz a hora tambem. -->
                <DataGridTextColumn Header="{DynamicResource Online_ColStateSince}" Width="95" Binding="{Binding StateSince}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="ToolTip" Value="{Binding StateSinceFull}"/></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="{DynamicResource Online_ColStartDate}" Width="95" Binding="{Binding StartDate}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColTargetDate}" Width="85" Binding="{Binding FinishDate}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColSprint}" Width="120" Binding="{Binding Sprint}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColOwner}" Width="170" Binding="{Binding AssignedTo}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColEstHours}" Width="90" Binding="{Binding EstimateHours}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="Margin" Value="0,0,6,0"/></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="{DynamicResource Online_ColDoneHours}" Width="90" Binding="{Binding CompletedHours}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="Margin" Value="0,0,6,0"/></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="{DynamicResource Online_ColTags}" Width="140" Binding="{Binding Tags}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColDescription}" Width="200" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="{DynamicResource Online_ColLastHistory}" Width="320" MinWidth="200" Binding="{Binding LastHistory}"/>
                <DataGridTemplateColumn Header="{DynamicResource Online_ColActions}" Width="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        Visibility="{Binding IsPending, Converter={StaticResource BoolToVis}}">
                                <Button Content="{DynamicResource Online_Edit}"
                                        Width="56" Height="22"
                                        Margin="2,0"
                                        Click="OnEditPendingClick"
                                        Tag="{Binding}"/>
                                <Button Content="{DynamicResource Online_Delete}"
                                        Width="56" Height="22"
                                        Margin="2,0"
                                        Foreground="#A4262C"
                                        Click="OnDeletePendingClick"
                                        Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsPending}" Value="True">
                            <Setter Property="Background" Value="#FFF8E1"/>
                            <Setter Property="FontStyle" Value="Italic"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Barra de criação -->
        <Border Grid.Row="2"
                BorderBrush="#DCDCDC"
                BorderThickness="0,1,0,0"
                Margin="0,10,0,0"
                Padding="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{DynamicResource Online_NewActivity}"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"/>
                <TextBox x:Name="NewNameBox"
                         Grid.Column="1"
                         Height="26"
                         VerticalContentAlignment="Center"
                         Padding="4,0"
                         KeyDown="OnNewNameKeyDown"/>
                <Button Grid.Column="2"
                        Content="{DynamicResource Online_AddPending}"
                        Height="28"
                        Margin="8,0,0,0"
                        Padding="10,0"
                        Click="OnAddPendingClick"/>
            </Grid>
        </Border>

        <!-- Rodapé -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock x:Name="LegendText"
                       Text="{DynamicResource Online_Legend}"
                       VerticalAlignment="Center"
                       FontSize="10"
                       Foreground="#666"
                       Margin="0,0,16,0"/>
            <Button x:Name="ShowOnBoardButton" Content="{DynamicResource Online_ShowOnBoard}"
                    Height="30" Padding="10,0" Margin="0,0,8,0" IsEnabled="False"
                    Click="OnShowOnBoardClick" ToolTip="{DynamicResource Online_ShowOnBoardTip}"/>
            <Button x:Name="OpenDevOpsButton" Content="🔗" FontSize="13" Width="34" Height="30"
                    Margin="0,0,8,0" IsEnabled="False" Click="OnOpenInDevOpsClick"
                    ToolTip="{DynamicResource Online_OpenDevOpsTip}"/>
            <Button Content="{DynamicResource Online_Close}" Width="90" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
