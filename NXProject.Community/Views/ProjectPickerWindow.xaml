<Window x:Class="NXProject.Views.ProjectPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selecionar Projetos do Portfólio"
        Width="700" Height="560"
        MinWidth="520" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        Background="White">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Selecionar Projetos do Portfólio"
                   FontSize="16" FontWeight="SemiBold" Foreground="#2B579A"
                   Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1"
                   Text="Marque os projetos que deseja incluir no Mapa de Alocação."
                   FontSize="11" Foreground="#666" Margin="0,0,0,10"/>

        <!-- Busca + botões -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0"
                     Height="28" Padding="6,0" VerticalContentAlignment="Center"
                     FontSize="12"
                     TextChanged="OnSearchChanged"/>
            <Button Grid.Column="1" Content="Limpar" Width="60" Height="28" Margin="6,0,0,0"
                    Click="OnClearSearchClick"/>
            <Button Grid.Column="2" Content="+ Adicionar arquivo..." Height="28" Padding="8,0" Margin="6,0,0,0"
                    Click="OnAddFileClick"/>
            <Button Grid.Column="3" Content="Remover da Lista" Height="28" Padding="8,0" Margin="6,0,0,0"
                    Click="OnRemoveFromListClick"/>
        </Grid>

        <!-- Lista de projetos com checkbox -->
        <Border Grid.Row="3" BorderBrush="#DDD" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ListBox x:Name="ProjectList"
                         SelectionMode="Single"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         FontSize="12"/>
            </ScrollViewer>
        </Border>

        <!-- Resumo -->
        <TextBlock x:Name="SummaryText" Grid.Row="4"
                   FontSize="11" Foreground="#555" Margin="0,8,0,0"/>

        <!-- Botões OK/Cancelar -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="OK" Width="80" Height="28" Margin="0,0,8,0"
                    IsDefault="True" Click="OnOkClick"/>
            <Button Content="Cancelar" Width="80" Height="28"
                    IsCancel="True" Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>
