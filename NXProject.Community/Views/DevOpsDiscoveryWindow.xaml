<Window x:Class="NXProject.Views.DevOpsDiscoveryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Discovery — Projetos do Azure DevOps"
        Width="640" Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinWidth="500" MinHeight="360"
        Background="White">

    <Border Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Título -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="Discovery — Projetos do Azure DevOps"
                           FontSize="16" FontWeight="SemiBold" Foreground="#2B579A"/>
                <TextBlock x:Name="SubtitleText"
                           Text="Conectando ao Azure DevOps..."
                           FontSize="12" Foreground="#666" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Status de carregamento -->
            <Border x:Name="LoadingPanel" Grid.Row="1" Background="#EFF3FB" BorderBrush="#C7D8F0"
                    BorderThickness="1" CornerRadius="6" Padding="10,8" Margin="0,0,0,10"
                    Visibility="Visible">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="StatusText" Text="🔄 Buscando work items raiz no DevOps..."
                               FontSize="12" Foreground="#2B579A" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Grid com checkboxes -->
            <DataGrid x:Name="ItemsGrid"
                      Grid.Row="2"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      BorderBrush="#D9E1EA"
                      BorderThickness="1"
                      RowBackground="White"
                      AlternatingRowBackground="#F9FAFB"
                      ColumnHeaderHeight="32"
                      RowHeight="30"
                      IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Adicionar" Width="70" CanUserResize="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Tipo" Binding="{Binding Type}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Nome no Azure DevOps" Binding="{Binding Title}" Width="*" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Seleção rápida -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,6,0,0">
                <TextBlock x:Name="CountText" Text="" FontSize="11" Foreground="#555" VerticalAlignment="Center"/>
                <Button Content="Marcar todos" Width="100" Height="26" Margin="12,0,0,0"
                        FontSize="11" Click="OnSelectAllClick"/>
                <Button Content="Desmarcar todos" Width="110" Height="26" Margin="6,0,0,0"
                        FontSize="11" Click="OnDeselectAllClick"/>
            </StackPanel>

            <!-- Botões OK/Cancelar -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="AddButton" Content="➕ Adicionar selecionados ao Portfólio"
                        Height="32" Padding="14,0" IsDefault="True"
                        Background="#2B579A" Foreground="White"
                        Click="OnAddClick" IsEnabled="False"/>
                <Button Content="Cancelar" Width="100" Height="32" Margin="10,0,0,0"
                        IsCancel="True" Click="OnCancelClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
